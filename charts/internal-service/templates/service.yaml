apiVersion: v1
kind: Service
metadata:
  name: {{ include "internal-service.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "internal-service.name" . }}
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
